{
  "name": "fichalba",
  "version": "0.2.0",
  "private": true,
  "description": "Fichaje de entrada del taller: una aplicación web que vive dentro de la tablet",
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "test": "node --no-warnings --test tests/*.test.js",
    "app": "node --no-warnings servidor/scripts/servir-app.js",
    "test-servidor": "node --no-warnings --test servidor/tests/*.test.js"
  }
}
