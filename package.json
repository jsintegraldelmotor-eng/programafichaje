{
  "name": "fichalba",
  "version": "0.1.0",
  "private": true,
  "description": "Fichaje de entrada en la tablet del taller",
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node --no-warnings src/servidor.js",
    "primer-arranque": "node --no-warnings scripts/primer-arranque.js",
    "trabajador": "node --no-warnings scripts/trabajador.js",
    "copia": "node --no-warnings scripts/copia.js",
    "test": "node --no-warnings --test"
  }
}
