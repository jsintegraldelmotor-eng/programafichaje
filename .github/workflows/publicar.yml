# Publica la carpeta app/ como página web, para que la tablet la abra.
# Hace falta activarlo una vez: Settings -> Pages -> Source: GitHub Actions.

name: Publicar la aplicación

on:
  push:
    branches: [main, claude/exciting-ride-v08l7w]
    paths: ['app/**', '.github/workflows/publicar.yml']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publicar:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.despliegue.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: app
      - id: despliegue
        uses: actions/deploy-pages@v4
