/* La versión de la aplicación. Se enseña en la pantalla del jefe para poder
   responder a "¿se ha actualizado esto?" mirando, en vez de adivinando.
   Tiene que coincidir con la de sw.js — hay una prueba que lo comprueba. */
export const VERSION = 'v5';
